Crosshair.controllerPriority = true;
/*
if (image_index == 0 || image_index == 2) image_index = 2; // crowching right image
else image_index = 3; //crowching left image
*/
downReleased_bool = true; // when the stick is released, padDownReleased will be
    // called a single time
